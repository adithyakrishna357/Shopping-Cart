<section>
    <div class="containeer">
        <div class="row" >
            <div class="d-flex pe-5 pt-3">
                <a href="/admin/add-user" class="btn btn-success ms-auto">Add User</a>
            </div>
        </div>
        <div class="mt-3">
        <table class="table" id="productsTable">
  <thead>
    <tr>
      <th scope="col">User Name</th>
      <th scope="col">Email</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    {{#each users}}
    <tr>
      <td>{{this.name}}</td>
     <td>{{this.email}}</td>
      <td><a href="/admin/edit-user/{{this._id}}" class="btn btn-success">Edit User</a></td>
      <td><a href="/admin/delete-user/{{this._id}}" class="btn btn-danger" onclick=" return confirm('Are you want  to delete {{this.name}}')">Delete User</a></td>
    </tr>
    {{/each}}
  </tbody>
</table>
</div>
    </div>
</section>
<script>

  $(function (){
    $('#productsTable').DataTable();
  });
</script>